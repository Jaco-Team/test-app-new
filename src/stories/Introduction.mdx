import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="UI/Introduction" />

# Новый UI layer

Source of truth находится в `src/shared/ui`. Storybook показывает состояния и visual contracts.
